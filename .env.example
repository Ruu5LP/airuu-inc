# ローカルでClaude Code CLIを使うときに、このファイルを .env にコピーして値を埋める。
# .env は .gitignore 対象なのでコミットされない。
# Claude Code on the web（リモート環境）で使う場合はこのファイルではなく、
# Environmentの環境変数設定に DISCORD_WEBHOOK_URL を登録すること。

DISCORD_WEBHOOK_URL=
